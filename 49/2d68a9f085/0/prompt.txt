Work on Linear issue ABA-247:

<issue identifier="ABA-247">
<title>--profile both — report formatting, per-profile verdict, exit code</title>
<description>
**P2. Moves KR1.** Assemble the full report once all tiers exist.

* `--profile both` reports both profiles side-by-side.
* Per-profile verdict; non-zero exit code on any FAIL.

**Acceptance:** both profiles reported side-by-side; exit code correct (0 = no FAIL, non-zero = any FAIL).
</description>
<team name="Personal"/>
<label>model:sonnet</label>
<project name="AU passport/visa photo — pre-submission compliance">Local, offline tool to verify and auto-fix a photo against AU passport/visa specs before printing or uploading — no face data sent to any online service.</project>
<issue-relations>
<blocking>
<issue-ref identifier="ABA-248" title="Auto-fix F2+F3 — visa size-targeting, -o output, re-check + exclusions"/>
<issue-ref identifier="ABA-249" title="README — usage section for check.py"/>
</blocking>
<blocked-by>
<issue-ref identifier="ABA-243" title="Tier A checks — format, size, dimensions, colour"/>
<issue-ref identifier="ABA-245" title="Tier B checks — sharpness, brightness, background"/>
<issue-ref identifier="ABA-244" title="Tier C checks — face geometry (one face, head %, centring, eyes)"/>
</blocked-by>
<related><issue-ref identifier="ABA-250" title="Validate check.py + add tests against verified AU photo requirements"/></related>
</issue-relations>
</issue>